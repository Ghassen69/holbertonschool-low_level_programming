#include "holberton.h"
/**
 * main - entry point
 * print message
 * Return: success
 * _islower(int c) - lowercase letters
 */
int _islower(int c)
{
if ((c >= 'a') && (c <= 'z'))

return (1);

else

return (0);
}
